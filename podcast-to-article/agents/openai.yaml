interface:
  display_name: "Podcast to Article"
  short_description: "Fetch YouTube search results and transcript context for linked articles"
  default_prompt: "Use $podcast-to-article to fetch YouTube transcript context, then write the article with clickable timestamps."

policy:
  allow_implicit_invocation: true
